let car = "subaru";

console.log(car === "subaru" && car.length > 0);
console.log(car === "subaru" || car.length === 0);
console.log(car.includes("u"));

console.log(car.includes("z"));
console.log(car === "subaru" && car.length < 0);
console.log(car !== "subaru" || car.length < 0);
